{
  "name": "teste-googlo-api",
  "version": "1.0.0",
  "description": "Google Places API integration for Paraty businesses",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "node scripts/build.js",
    "test": "jest --detectOpenHandles",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:api": "jest src/api",
    "test:services": "jest src/services",
    "test:utils": "jest src/utils"
  },
  "jest": {
    "testEnvironment": "node",
    "verbose": true,
    "setupFiles": ["dotenv/config"]
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "axios-mock-adapter": "^1.22.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=22.14.0"
  }
}
